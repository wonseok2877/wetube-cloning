extends layouts/main.pug

block content
    .form-container
        form(action=routes.users.editProfile, method="post")
        label(for="avatar") Avatar
            input(type="file" id="avatar" name="avatar")